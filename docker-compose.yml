version: '3.9'

services:
  blippy:
    build: .
    container_name: blippy-node

    restart: unless-stopped

    ports:
      - "8000:8000"

    volumes:
      - .:/app

    stdin_open: true
    tty: true

    command: python main.py
