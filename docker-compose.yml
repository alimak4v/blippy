version: '3.9'

services:
  blippy:
    build: .
    network_mode: "host"
    privileged: true
    
    volumes:
      - /var/run/dbus:/var/run/dbus
      - ./database.db:/app/database.db

    stdin_open: true
    tty: true
